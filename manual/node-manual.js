const string = "string"
const integer = 12
const boolean = true // false
const operator = integer + integer

//gabungin string + variable (const,var,let)
console.log(`ini string/varchar (${string})`)

//if else + boolean scenario
const verifikasiPassword = false // true

if (verifikasiPassword) {
    console.log('Password Benar')
} else {
    console.log('Password Salah')
}